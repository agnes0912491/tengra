# Troubleshooting

## Common Issues
- Port already in use: stop the other process or change the port.
- Missing env vars: create `.env` from `.env.example`.
- Build failures: run `npm run lint` and fix errors first.

## Logs
- Check `data/logs` for recent run logs when available.

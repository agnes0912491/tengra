import { TranslationConfig } from "@tengra/language";

const config: TranslationConfig = {
  locales: ["tr", "en"],
  defaultLocale: "tr",
  translationsPath: "../../translations/apps/tengra-translations",
};

export default config;
